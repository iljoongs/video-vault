<Window x:Class="VideoVault.FolderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="폴더 목록" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="OpenFolderButton" Content="폴더 열기" Padding="10,5" Margin="0,0,5,0" Click="OpenFolder_Click"/>
            <Button x:Name="RefreshButton" Content="새로고침" Padding="10,5" Margin="0,0,5,0" Click="Refresh_Click"/>
            <Button Content="초기화" Padding="10,5" Margin="0,0,15,0" Click="ResetFolderList_Click"/>
            <TextBlock x:Name="FolderPathText" VerticalAlignment="Center" Foreground="Gray" Text="폴더를 선택하세요." TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <ListView x:Name="FolderListView" Grid.Row="1" SelectionMode="Extended">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="파일명" Width="260" DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Header="크기" Width="90" DisplayMemberBinding="{Binding SizeDisplay}"/>
                    <GridViewColumn Header="수정일" Width="140" DisplayMemberBinding="{Binding ModifiedDisplay}"/>
                    <GridViewColumn Header="전체 경로" Width="450" DisplayMemberBinding="{Binding FullPath}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="FolderFileCountText" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,15,0"/>
            <Button Content="파일 삭제" Padding="15,5" Margin="0,0,5,0" Click="DeleteFile_Click"/>
            <Button Content="관리 리스트에 추가 →" Padding="15,5" Margin="0,0,15,0" Click="AddToManagedList_Click"/>
            <Button Content="닫기" Padding="15,5" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
